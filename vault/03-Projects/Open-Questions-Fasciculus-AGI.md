# Open questions — fasciculus mesh & AGI stance

Answer these to tighten Cam’s connectome parameters. Defaults are already in `config/connectome/mesh-params.json`.

1. **Language lateralization** — Left-only AF (human-like) or bilateral redundancy for converse failover?
2. **Workspace count** — Max concurrent Cam workspaces before forceps_minor / mesh_callosal should lease-block?
3. **Fornix consolidation** — Auto-promote episodic vault notes → semantic distillates on a nightly schedule?
4. **Dual-stream conflict** — If dorsal AF and ventral EmC disagree on a speak act, which wins?
5. **ASI ceiling** — Hard cap on neurogenesis count and myelination so growth stays scrubber-inspectable?
6. **Tailscale as callosum** — Are iPhone/iPad/Mac forceps terminals of one brain, or separate brains linked only by `mesh_callosal`?

Reply in chat or edit `mesh-params.json` `open_questions_for_aaron` with `"answer"` fields.
