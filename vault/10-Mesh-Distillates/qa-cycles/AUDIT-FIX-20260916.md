# Audit fix cycle — 2026-09-16

Dispatched by continuous QA after [Audit connectome configs](bc-f71fed00-61df-5e3b-b77f-072ffbbb8e87).

## Fixed (high)

| Issue | Fix |
|---|---|
| `side_effects` bypassed switch holds | Route gates every motor via `requires_switch` + kill |
| `motor.mesh` ignored kill | Added `switch.kill` to `requires_switch` |
| Presence missing `switch.outbound` | Pathway: presence → outbound → speak |
| Vision/jarvis fired mesh without autonomy | Inserted `switch.autonomy` before `motor.mesh` |
| Recall hotspots motor-only via side_effects | Pathway now ends `switch.autonomy → motor.mesh` |
| Feedback edges invented in code | Real `motor.* → center.memory` (+ speak self-hear) synapses |
| Route soft-skipped motor/switch edges | Full consecutive-edge check |
| Simulator integrity weak | Pathway motors must include required switches; feedback edges scanned |

## Regression checks
- `--no-autonomy` docs/careers → empty `motor_plan`
- `--kill` → empty motors
- `--strict-edges` 2M sims → 0 fail, 0 missing edges

## Still suggested
- Static graph checker as CI default; keep billion fuzz as stress
- Shared `validate_pathway()` module for route + sim
- Traffic-weighted sense sampling
- Mirror QA cycle events into mesh persistence
