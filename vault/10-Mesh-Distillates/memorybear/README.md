# MemoryBear distillates

Cognitive memory read/write packs from `scripts/memorybear.py` land here
(day folders). Prefer mesh claims via `scripts/pack-memorybear-result.py`
before promoting into `mesh/facts`.

See `config/integrations/memorybear.md`.
